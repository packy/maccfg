#!/bin/sh
if [[ ! -e $TMPDIR/emacs501/server ]]; then
  # launch the editor
  /Applications/Emacs.app/Contents/MacOS/Emacs $* &
  # wait for the server to start up
  while [[ ! -e $TMPDIR/emacs501/server ]]; do sleep 1; done
fi
